# BEMPIC v0.1 conformance tranche 3 work report

Date: 2026-09-01  
Repository: `Gordonfive/bempic-reference`  
Branch: `codex/v0.1-conformance-tranche-3`  
Base: freshly fetched `origin/main` at
`cf3485f6606d6462077e8edd1592264c3ce4ca5e`  
Normative input: `Gordonfive/bempic` at
`10fc1ddca0b16c974d29a24b6ff2bef189663a1f` (read-only)  
Application-profile input: `Gordonfive/oceanmail` at
`cc55c1b7d5a03aa2e5cc8cd617f9d1bb7b6a3600` (read-only)  
Status: local evidence complete; push, pull request, exact-head CI, and review
audit pending; no conformance, merge, tag, or release claim

## Assignment scope

Implement the clarified normative `semantic_bytes` definition and directional
accounting; publish the exact mandatory 24-row V08 interruption/restart/storage
matrix; substantially complete V01--V15 with executed valid, boundary,
malformed, retry, stale-cache, duplicate, conflict, and unrelated-state traces;
add full-width and one-past fixtures; consume OceanMail immutable-object
semantics without importing OceanMail policy into BEMPIC; preserve the compact
codec under private IDs `0xffff0001/2`; and refresh deterministic measurements,
digests, catalog/report status, and independent Python verification.

The specification and OceanMail Git objects were inspected with `git show` and
`git ls-tree` only. No sibling branch, worktree, registry, or source file was
modified.

## Architecture and boundary decisions

- One measurement scope binds endpoint A and endpoint B once. `send` means A to
  B and `receive` means B to A in every reporter.
- `semantic_bytes` is the recursive sum of decoded application scalar values.
  It counts fixed-width integer/enum/octet-string values by their normative
  decoded widths and UTF-8 strings by normalized UTF-8 octets. The
  representation-descriptor container and every descriptor member contribute
  zero. Framing, repeats, retransmissions, overlap, unselected representations,
  and lower-layer bytes contribute zero.
- `SemanticAccounting` counts a `(direction, representation_id)` only at its
  first accepted application selection. An identical repeat is idempotent; a
  repeat with a different semantic count fails closed; checked `u64` addition
  prevents evidence-domain overflow.
- `ImmutableObjectRegistry` stores only an opaque 32-octet application-supplied
  immutable-semantics digest keyed by the 32-octet object ID. A matching repeat
  is idempotent, a conflict is rejected without overwrite, and unrelated state
  remains usable. OceanMail owns normalization, object-ID generation, mailbox
  policy, and receipt meaning.
- The V08 matrix executes the specification's exact 24 named rows. Its storage
  surfaces are a bounded in-process buffer, a fault-injected representation
  part file, and the durable two-slot store. Every row records the durable state
  before interruption, recovered prefix, first resumed offset, new and duplicate
  payload, receipt state, final digest/ID/decode, and a deterministic trace
  digest.
- `CodecIdentity` makes evidence regeneration accept an explicit future
  ID/revision. Defaults remain implementation-local private-use
  `0xffff0001/2`; no specification allocation is predicted or claimed.
- V01 and V02 remain blocked as public byte evidence despite passing local
  behavior because their bytes use the private codec. V09 remains blocked on
  external M4P review despite passing the local opaque-carrier trace.

## Files and behavior changed

- `.github/workflows/ci.yml`: verifies the tranche-3 bundle on every supported
  CI platform.
- `Cargo.lock` and `crates/bempic-conformance/Cargo.toml`: add the existing
  workspace store crate to the evidence runner; no third-party dependency was
  added.
- `crates/bempic-conformance/src/main.rs` and new `tranche3.rs`: add
  deterministic write, display, and verify commands; execute V01--V15 evidence,
  the 24 V08 rows, 42 V12 budget cases, 26 V15 failure cases, fixtures,
  measurements, and RFC 8785 bundle construction.
- `crates/bempic-model/src/lib.rs`: target the clarified specification commit;
  implement directional semantic accounting, exact manifest/opaque semantic
  sizing, pre-allocation manifest bound validation, and the policy-free
  immutable-object registry with tests.
- `crates/bempic-sync/src/v01_compact.rs`: add explicit allocation-ready codec
  identity parameters while preserving the private default and strict
  canonical aliases.
- `scripts/verify_conformance.py`: independently verify strict JSON, bundle and
  file digests, RFC 8785 schema fingerprints, full-width IDs, semantic totals,
  OceanMail immutable digest, V01--V15 inventories/outcomes, all V08 trace
  digests, all FAILURE records, measurements, and the prior compact pack.
- `test-vectors/v0.1-experimental/catalog.json`, `manifest.json`, new
  `tranche3-evidence.json`, new message-manifest schema, and new
  `semantic-fixtures/`: publish the deterministic 12-pass/3-blocked catalog,
  full-width fixtures, and executed evidence.
- `benchmarks/results/conformance-tranche-3-2026-09-01.json`: publish all 18
  required accounting metrics, private compact measurements, and case counts.
- `conformance/v0.1.0-report.json` and `conformance/fuzz-report.json`: update
  normative provenance, exact evidence, dependency digest, malformed corpus,
  requirement statuses, and blockers.
- `README.md`, `docs/ARCHITECTURE.md`,
  `docs/CONFORMANCE-EVIDENCE-v0.1.md`, `docs/RELEASES.md`, and
  `test-vectors/README.md`: document the semantic boundary, vector coverage,
  private-codec status, reproduction commands, and remaining release blockers.
- This report records the full assignment, verification, failures, and handoff.

## Deterministic artifacts and digests

The RFC 8785 domain-separated bundle digest is
`f27d2086322c4dfaae2608e3fd4afcadfe0671d70bbccf7ae07543c05cd8544c`.
Deterministic regeneration produced the same digest and these eight manifest
entries:

| Path | Octets | SHA-256 |
| --- | ---: | --- |
| `catalog.json` | 3,692 | `23c8b1b766fcb33d987871b45e12eb15109a173d0640e695e1573ff76fdcea48` |
| `schemas/core-operations.schema.jcs` | 2,390 | `fd9c24a272c8d1ee1479b2f54d8a6accdc39818d0031029f7619f45f35648353` |
| `schemas/message-manifest.schema.jcs` | 1,915 | `b2f0ce7e911836bbb47a9d2276d1e81f6bb4042c899a5e5902a675f678c4799e` |
| `schemas/opaque-binary.schema.jcs` | 215 | `40c7d501558d7f49a174374bfd6b37d7da210eb8f6e5a23b1852b07ed776e9d3` |
| `semantic-fixtures/ack-response.json` | 113 | `f88231b535aa1d1f5a580d6ddcc5689f5cbc34e9028f9e1e6c9a631201652443` |
| `semantic-fixtures/message-manifest-full-width.json` | 6,643 | `568796cdd615b8ea5975a373b4f276c2e0e2190a815c635c521fcaf915b8e542` |
| `semantic-fixtures/oceanmail-immutable-object-v1.json` | 771 | `44d5fe151a0addb65ac53c1074a5a112b19b97b1e7b8ef775a32f3a1dfd2275d` |
| `tranche3-evidence.json` | 79,699 | `fb977f03181ca86081248e61c896ce0f5987e08aa54ffc3bb6d9aa6c68730e2e` |

Published RFC 8785 schema fingerprints are:

- core operations:
  `c4a686e7e9c6a40a5f187259a376b26cfc1d355179fd9fff487e105aeeac7302`;
- message manifest:
  `0ac001efba42837aade054401d9d307d16ad4715feac288fcb3d1711e4b961da`;
- opaque binary:
  `d8906a1cefbf89e4f29b4a0f636cfbfa1e9c6301e7e3a4fe213c090066f8e797`.

The independent verifier also reproduced the prior private compact artifact
digest `fd3461c674921b9730c773f0eef01d34dcc8e60a472ca1bb2ec1f3027de2f525`
and vector-pack digest
`5edd10847be60cef384be726d7f3d83c6d78618ec38ce56db469cf24e794b8fb`.
The malformed seed corpus digest is
`1442397586ab6af7c58543f7acd5d29d9812067dbc8bf8ff0ae1562b4b968ad7`.
The final dependency lock digest is
`1575d0c14e771306cb951f1af8cec83cb51dbf526f3f53d13668159e1dd2b1a4`.

## Measurements and conformance counts

### Before and after

| Evidence | Before | After |
| --- | --- | --- |
| Mandatory V01--V15 catalog | 6 pass, 9 partial | 12 pass, 0 partial, 0 fail, 3 blocked |
| Requirement report (76 total) | 52 pass, 14 partial, 4 fail, 6 blocked | 64 pass, 5 partial, 1 fail, 6 blocked |
| V08 interruption rows | incomplete aggregate coverage | 24 exact executed rows |
| V12 budget cases | aggregate exact-fit/one-short tests | 42 cases: 7 operations x 3 domains x 2 boundaries |
| V15 failure cases | codes round-tripped without complete state traces | 26: 13 codes x both advertised retry flags |

The one remaining `fail` is `ACCEPT-01`: not every correctness/release item is
pass. The six blocked requirements remain explicit. No claimed class or profile
was added.

### Exact no-change measurements

For the prescribed equal 100-message case:

| Encoding | Warm | Cold | Gate | Result |
| --- | ---: | ---: | ---: | --- |
| Existing B1 comparison | 88 B | 292 B | 64 B / 128 B | fail |
| Private compact candidate `0xffff0001/2` | 35 B | 75 B | 64 B / 128 B | pass locally; nonconformant until allocation |

### Semantic and directional accounting

The full-width semantic fixture counts 148 application octets in the send
direction and the ACK fixture counts 3 in receive, for exactly 151. Repeating
the selected send representation contributes zero additional semantic bytes.
Changing any representation-descriptor field does not change its manifest
semantic count.

The integrated 2,048-octet body measurement records:

| Metric | Exact value |
| --- | ---: |
| `semantic_bytes` / send / receive | 2,048 / 2,048 / 0 |
| `bempic_total_bytes` / send / receive | 3,538 / 2,855 / 683 |
| representation payload / useful committed | 2,048 / 2,048 |
| duplicate BEMPIC / duplicate payload | 0 / 0 |
| unselected payload | 0 |
| retransmitted durable prefix / prior manifest | 0 / 0 |
| resume control | 1,490 |
| bytes to first body payload / commit | 747 / 3,447 |
| preflight quote / quote error | 3,538 / 0 |
| carrier bytes / precision | 3,658 / exact |
| link cost | unavailable, explicitly labeled |

## Verification commands and exact results

All commands ran from this repository. Rust commands used the pinned Rust
1.81.0 toolchain; Python used 3.12.13.

- `cargo fmt --all -- --check` -- exit 0, no differences.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` --
  exit 0; finished the dev profile with no warning.
- `cargo test --workspace --all-targets --all-features --no-fail-fast` -- exit
  0; 66 passed, 0 failed: carrier 1, codec 2, cross-language 1, conformance 4,
  model 8, simulator 14, store 17, sync 19.
- `cargo test --workspace --all-features --doc` -- exit 0; six crate doc-test
  targets passed, with no doctest cases defined.
- `cargo run -q -p bempic-conformance --release` -- exit 0; 50,000 random,
  187 B1 structured malformed, 175 compact structured malformed, 4,100 B1
  exact-size, and 4,100 compact exact-size cases; zero panics, unexpected
  accepts, round-trip failures, compact round-trip failures, or unresolved
  findings. The final observed runtime was 73 ms; the committed audit artifact
  records its own 75 ms run.
- `cargo run -q -p bempic-conformance --release -- tranche2-measurements` --
  exit 0; reproduced B1 88/292, integrated 3,538 BEMPIC octets, exact
  reconstruction, zero duplicates and quote error, and 1,082 carrier-octet /
  401 payload-octet persistent-resume savings.
- `cargo run -q -p bempic-conformance --release --
  write-tranche3-evidence` -- exit 0; wrote 10 generated artifacts and reported
  bundle digest `f27d2086...544c`.
- `cargo run -q -p bempic-conformance --release --
  verify-tranche3-evidence` -- exit 0; verified 10 files, 24 V08 rows, 26 V15
  cases, and exact bundle digest `f27d2086...544c`.
- `cargo run -q -p bempic-conformance --release --
  verify-compact-codec-evidence` -- exit 0; pass, 35 B warm, 75 B cold, seven
  maximum witnesses.
- `cargo run -q -p bempic-cli -- demo` -- exit 0; complete, 1,046 BEMPIC
  octets, zero duplicate payload, exact reconstruction after seven reopens.
- `cargo run -q -p bempic-bench --release` -- exit 0; six fixtures, 23,334
  representation octets, 25,510 BEMPIC octets, zero duplicate payload in every
  fixture, exact reconstruction in every fixture.
- `python -m pip check` -- exit 0; no broken requirements.
- `python -m unittest discover -s prototype -p test_*.py -v` -- the approved
  rerun exited 0; 23 passed in 5.540 s. See F09 for the sandboxed first attempt.
- `python -m prototype.demo` -- exit 0; complete; 398-byte manifest and
  1,023-byte selected attachment reconstructed; zero deferred attachment bytes
  before selection, zero duplicate payload, zero quote error.
- `python -m prototype.benchmark` -- exit 0; six fixtures; 24,984 measured and
  predicted exchange bytes, zero quote error and duplicate payload; warm/cold
  legacy summary 58/76; B2F explicitly `not yet integrated`.
- `python scripts/verify_conformance.py` -- exit 0; independently verified the
  exact bundle/compact digests, three schema fingerprints, 151 = 148 + 3
  semantic octets, 24 V08 rows, 42 V12 cases, 26 V15 cases, 18 metrics, and
  compact 35/75 measurements.
- `python scripts/check_docs.py` -- exit 0; 76 conformance requirements, 18 JSON
  files, 16 Markdown files, and 12 local Markdown links passed.
- `python -m py_compile scripts/verify_conformance.py scripts/check_docs.py
  prototype/demo.py prototype/benchmark.py` -- exit 0, no output.
- `git diff --check` -- exit 0, no whitespace errors before report finalization.

## Commits and branch

- Branch: `codex/v0.1-conformance-tranche-3`.
- Implementation commit:
  `a94ecf83a312d5f2bce30b8c84a9581e170a4bec` (`Implement conformance tranche
  3 evidence`).
- Report/metadata commit: pending pull-request and CI completion.

## GitHub Actions, pull request, and review status

Pending commit, push, non-draft pull request, exact-head Ubuntu/Windows/macOS
and Python CI, and valid-review-thread resolution. The workflow now invokes the
tranche-3 verifier on every Rust platform. This section will be updated with
the PR, exact head, run/job URLs, and review outcome before handoff.

## Remaining blockers and deferred work

- The specification registry has no experimental compact-codec allocation;
  V01/V02 public bytes, `CODEC-01`, and `REF-09` remain blocked.
- No approved legally compatible B2F/LZHUF oracle exists; `REF-14` and
  `ACCEPT-08` remain blocked and no result was fabricated.
- External M4P maintainer binding review is unresolved; V09 and `ACCEPT-09`
  remain blocked even though the local opaque-carrier behavior passes.
- Application-profile acceptance/delivery transitions, conforming manifest
  codec integration, and maintainer-accepted transitional-proof parity remain
  partial.
- An independent implementation has not reproduced the bundle, and the
  immutable reference commit has not been accepted into a specification
  release record.
- Release-candidate and release gates remain open. This branch must not be
  merged, tagged, or released by this assignment.

## Notes affecting sibling repositories

- The BEMPIC specification registry remains unchanged. The allocation-ready
  evidence can accept a future specification-owned ID/revision, but it does not
  reserve one.
- OceanMail remains owner of application object identity, normalization,
  immutable-semantics composition, mailbox behavior, and delivery policy.
  BEMPIC receives only an opaque digest at its conflict-detection boundary.
- M4P remains owner of routing, forwarding, TTL, custody, fragmentation, and
  lower-layer retransmission. No M4P or DCCL/M4P code, table, protected
  material, or incompatible dependency was copied.

## Failures and recoveries

All excerpts are sanitized. Stable sequence IDs are used instead of timestamps.

### F01 -- combined audit output was truncated

- Sequence: F01; action: repository-wide term search and multi-file audit.
- Exit/result: command exit 0, but the display reported truncated output, so it
  was not accepted as complete evidence.
- Root cause: requested output exceeded the display budget.
- Corrective action: reread authoritative documents and implementations in
  bounded chunks and use focused symbol searches.
- Verification: the normative metric definition, exact V08 table, vector
  requirements, store primitives, and OceanMail digest boundary were recovered.
- Final status: resolved.

### F02 -- first focused Rust compile found type/import errors

- Sequence: F02; command: format, then focused all-feature tests.
- Exit code: 1 during compilation.
- Excerpt: `FailureCode: StdError is not satisfied`, plus unused imports.
- Root cause: the protocol `FailureCode` enum was propagated into
  `Box<dyn Error>` and initial evidence imports were unused.
- Corrective action: convert unexpected negotiation failures to explicit
  diagnostics and remove unused imports.
- Verification: final focused and workspace runs pass; conformance 4, model 8,
  sync 19, and all 66 workspace tests are green.
- Final status: resolved.

### F03 -- corrective patch used pre-format context

- Sequence: F03; action: combined source/report `apply_patch`.
- Exit/result: patch rejected atomically; no partial write.
- Excerpt: `Failed to find expected lines ... tranche3.rs`.
- Root cause: rustfmt changed import wrapping.
- Corrective action: inspect current lines and apply smaller exact-context
  patches.
- Verification: intended source and report changes are present.
- Final status: resolved.

### F04 -- first strict Clippy pass found evidence-builder style issues

- Sequence: F04; command: strict Clippy on changed crates.
- Exit code: 101.
- Excerpt: unreadable numeric literal, three functions over the 100-line lint
  threshold, and an uninlined format argument.
- Root cause: exhaustive JSON builders were deliberately linear; two mechanical
  forms did not match workspace pedantic style.
- Corrective action: use narrow `too_many_lines` allowances only on exhaustive
  builders, group the literal, and inline the format argument.
- Verification: final workspace Clippy with `-D warnings` exits 0.
- Final status: resolved.

### F05 -- executed-trace patch used stale context

- Sequence: F05; action: one large patch for V02/V03/V04/V11/V12 and fixtures.
- Exit/result: patch rejected atomically.
- Excerpt: `Failed to find expected lines ... one-past-representation-allocation`.
- Root cause: rustfmt wrapping differed from one hunk.
- Corrective action: split into small exact-context patches.
- Verification: all intended executed traces and fixture corrections are in the
  generated bundle and pass both verifiers.
- Final status: resolved.

### F06 -- second strict Clippy pass found remaining local style issues

- Sequence: F06; command: format, strict Clippy, then focused tests.
- Exit code: Clippy 101; the enclosing PowerShell sequence later returned 0
  because subsequent tests passed, so the Clippy sub-result was audited
  separately.
- Excerpt: two exhaustive builders over 100 lines and two
  `assigning_clones` findings.
- Root cause: V04/V11 grew when summary cases became executed traces; two string
  replacements used assignment syntax.
- Corrective action: add two narrow length allowances and use `clone_into`.
- Verification: standalone workspace Clippy exits 0 with `-D warnings`.
- Final status: resolved.

### F07 -- independent verifier expected the wrong V04 inventory

- Sequence: F07; command: Python compilation then independent verifier.
- Exit code: 1 from verification; compilation passed.
- Excerpt: `VerificationError: V04 one-past rejection mismatch`.
- Root cause: the verifier required 13 invalid cases while the generator
  intentionally publishes 12, covering each clarified scalar, count, nesting,
  codec-parameter, representation-allocation, and manifest-allocation boundary
  exactly once.
- Corrective action: correct the independent inventory threshold to 12; no
  outcome, bound, or rejection behavior was weakened.
- Verification: independent verifier exits 0 and checks all 12 rejected cases.
- Final status: resolved.

### F08 -- combined final verification output was truncated

- Sequence: F08; action: one call running compact verification, tranche-3
  verification, demo, and benchmark.
- Exit/result: display reported output exceeded available context; individual
  command status was not accepted.
- Root cause: four JSON outputs exceeded the combined display budget.
- Corrective action: rerun all four commands separately with bounded output.
- Verification: each separate command exited 0 with the exact results recorded
  above.
- Final status: resolved.

### F09 -- sandboxed Python filesystem tests could not use host temp

- Sequence: F09; command: `python -m unittest discover -s prototype -p
  test_*.py -v` under the default filesystem sandbox.
- Exit code: 1; 23 tests ran, 12 passed and 11 filesystem tests errored.
- Excerpt: `PermissionError: [WinError 5] Access is denied` under the Windows
  host temporary directory.
- Root cause: the sandbox allowed the repository but not the Python runtime's
  selected host temp directory; no assertion failed.
- Corrective action: rerun the identical command with approved host-temp access.
- Verification: exit 0; all 23 tests passed in 5.540 seconds.
- Final status: resolved.

### F10 -- full report replacement used two operations on one path

- Sequence: F10; action: one `apply_patch` that deleted and added the work
  report in the same patch.
- Exit/result: patch validation failed atomically; no partial write.
- Excerpt: `multiple operations target ... conformance-tranche-3.md`.
- Root cause: the patch engine permits only one operation per path in a patch.
- Corrective action: issue a delete patch followed by an add patch.
- Verification: this complete replacement report is present and documentation
  checks are rerun after finalization.
- Final status: resolved.

### F11 -- final audit found aggregate semantic-counter overflow

- Sequence: F11; action: pre-commit review of `SemanticAccounting`.
- Exit/result: no command failed; review found that each directional counter was
  checked independently while their public total still used direct addition.
- Root cause: the first implementation did not enforce the combined `u64`
  evidence-domain bound when the opposite direction was updated.
- Corrective action: check `send + receive + new_selection` before any counter
  or selection-map mutation, and add a `u64::MAX` send plus one-byte receive
  rejection assertion.
- Verification: strict workspace Clippy passes; all 66 tests pass; both Rust and
  independent Python bundle verifiers retain the exact `f27d2086...544c`
  digest and 148 + 3 semantic totals.
- Final status: resolved.

No secrets, protected third-party material, sibling writes, registry changes,
merge, tag, or release occurred.
