# BEMPIC Reference v0.1.0 foundation work report

**Date:** 2026-08-31  
**Branch:** `codex/bempic-reference-v0.1.0`  
**Status:** implementation delivered to PR; local and initial cross-platform CI verification complete; final report-commit CI pending

This pushed Git document is the authoritative work record for the assignment.

## Assignment scope

Build the Apache-2.0 BEMPIC Reference v0.1.0 foundation from the read-only
`C:\Projects\OceanMail\bempic` specification/oracle source. Reproduce the
Python behavioral proof; create a Rust workspace for deterministic preparation,
identities, offers, requests, transfer, semantic receipts, persistence,
interruption, reopen, resume, and exact reconstruction; add a deterministic
carrier simulator, fixtures, differential vectors, three-platform CI, demos,
and benchmarks; then commit, push, and open a pull request.

Explicit exclusions were radios, M4P implementation, Mailcow, billing,
production authentication, and production cryptography. DCCL was to remain
prior art only, without a dependency or its wire format. No stable BEMPIC wire
format could be frozen by this release.

## Files and behavior changed

- Adopted Apache-2.0 in `LICENSE`; added workspace/package license metadata.
- Reproduced the complete tracked Python semantic oracle under `prototype/`,
  including tests, fixtures, demo, benchmark, and the recorded baseline.
- Created the Rust workspace and pinned Rust-1.81-compatible dependencies:
  - `bempic-model`: declarative object bounds, logical/part/representation
    identifiers, prepared bytes, attachment separation, schema fingerprints;
  - `bempic-codec`: pluggable codec trait, exact encoded-size analysis,
    conservative maximum size, strict generation-0 encode/decode;
  - `bempic-sync`: capabilities, summaries, deterministic collection digest,
    offers/pagination, requests, offset data, semantic receipt stages, exact
    operation and direction accounting;
  - `bempic-store`: crash-conscious JSON state, durable prefix bytes,
    idempotent duplicates, collision rejection, integrity quarantine, reopen,
    resume, commit, and complete-file re-verification;
  - `bempic-carrier`: narrow complete opaque-record carrier contract;
  - `bempic-sim`: deterministic byte budgets, record ceiling, bandwidth,
    latency, abrupt disconnects, elapsed time, event log, and separate carrier
    metrics plus a reopen-before-every-contact transfer runner;
  - `bempic-cli`: interruption/resume demo, strict inspection, vector output;
  - `bempic-bench`: deterministic synthetic-corpus transfer measurements.
- Added non-normative Rust/Python differential vectors under `test-vectors/`
  and a test in each language that checks exact bytes, sizes, identities, and
  digests.
- Added architecture/release documentation and Windows/Linux/macOS GitHub CI.
- Added this permanent repository reporting policy in `AGENTS.md`.

## Architecture and boundary decisions

The preserved dependency direction is:

```text
OceanMail → BEMPIC → M4P → DataLink
```

BEMPIC owns compact application representations, discovery/synchronization,
application byte budgets, persisted continuation, integrity verification, and
semantic application receipts. M4P continues to own addressing, routing,
store-carry-forward, network TTL, generic fragmentation, deduplication, and
cross-modality behavior. DataLink continues to own waveforms, FEC, ARQ, modem
timing, and physical-byte costs. The deterministic simulator models only the
opaque carrier contract and does not implement either lower layer.

Generation-0 markers, integer widths, 16-byte identifiers, SHA-256 use, schema
fingerprints, and operation assignments are explicitly experimental. SHA-256
is used only for deterministic content identity and corruption detection; it
is not authentication or confidentiality. The codec interface borrows DCCL's
useful *properties*—declarative bounds, maximum encoded size, fingerprints,
pluggability, and exact byte analysis—without a DCCL dependency or wire format.

## Commits and branch

- Branch: `codex/bempic-reference-v0.1.0`
- `0ecf0df` — `feat: build BEMPIC reference v0.1.0 foundation` (implementation,
  Python oracle, fixtures, CI, local verification record, and reporting policy).
- `498664e` — `docs: record v0.1.0 foundation verification` (implementation
  commit identifier and final local results).
- `d80df9f` — `docs: record GitHub push permission blocker` (required failed
  push record, exact remote, and authorization requirement).
- `docs: finalize v0.1.0 delivery report` — this final report update; its
  immutable hash is the PR head created by the next push.

## Verification commands and exact results

Authoritative final local results:

| Command | Exact result |
|---|---|
| `cargo check --workspace --all-targets` | PASS after recovery; exit 0; all eight workspace packages checked. |
| `cargo fmt --all -- --check` | PASS; exit 0; no formatting differences. |
| `cargo test --workspace --all-features` | PASS; exit 0; 12 Rust unit/integration/differential tests passed, 0 failed; all doc tests passed. |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | PASS after recovery; exit 0; no warnings. |
| `cargo run -q -p bempic-cli -- vectors` | PASS; exit 0; produced 105-byte experimental vector with representation ID `f1a78ed3691014a9a0fbd54521e2b3a4`. |
| `cargo run -q -p bempic-cli -- demo` | PASS; exit 0; complete exact 338-byte reconstruction after 8 contacts/7 reopens; 1,046 BEMPIC bytes, 1,166 carrier bytes, 17,696 simulated ms, 0 duplicate persisted payload bytes, 2 disconnect-lost records/124 lost BEMPIC bytes. |
| `cargo run -q -p bempic-bench --release` | PASS; exit 0; 6 fixtures, 23,334 representation bytes, 25,510 BEMPIC bytes, exact reconstruction for every fixture, 0 duplicate persisted payload bytes. |
| Python JSON comparison of `cargo run -q -p bempic-bench --release` against `benchmarks/results/baseline-2026-08-31.json` | PASS; exit 0; `benchmark baseline exact: PASS`. |
| `python -m unittest discover -s prototype/tests -v` | PASS; exit 0; 23 Python oracle/differential tests passed in 6.051 s, 0 failed. |
| `python -m unittest prototype.tests.test_proof.ExchangeTests.test_every_contact_respects_its_hard_budget -v` | PASS on isolated retry; exit 0; 1 test passed. |
| `python -m prototype.demo` | PASS; exit 0; status `complete`; manifest and selected attachment exact reconstruction, prediction error 0, duplicate payload 0, deferred attachment payload 0. |
| `python -m prototype.benchmark` | PASS; exit 0; 6 fixtures; 24,984 predicted/actual exchange bytes with 0 prediction error; warm no-change 58 bytes; one-new-object discovery 120 bytes. |
| Normalized-content comparison of all 17 reproduced oracle files against `C:\Projects\OceanMail\bempic` | PASS; exit 0; `oracle-files=17 normalized-content-differences=0`. Only repository-normalized line endings differ. |
| `git diff --check` | PASS; exit 0; no whitespace errors. |
| `git push -u origin codex/bempic-reference-v0.1.0` after exact-remote authorization | PASS; exit 0; new remote branch created and upstream tracking configured. |
| `gh pr create --repo Gordonfive/bempic-reference --base main --head codex/bempic-reference-v0.1.0 ...` | PASS; exit 0; created [PR #1](https://github.com/Gordonfive/bempic-reference/pull/1). |
| `gh pr checks 1 --repo Gordonfive/bempic-reference --watch --interval 10` | PASS; exit 0; all 8 initial push/PR-trigger checks completed successfully. |
| `gh pr view 1 ...` and `gh run view 33452249166`, `gh run view 33452275362` | PASS; exit 0; PR open/non-draft/mergeable/`CLEAN`; both runs completed with conclusion `success`. |

All required local checks, demos, and benchmarks are green. GitHub Actions and
pull-request status remain required after the first push.

## Failures and recoveries

Stable sequence identifiers are used because command-level UTC timestamps were
not captured by the execution host. No raw logs or secrets are committed.

| Seq. | Command/action and exit | Concise error | Root cause | Corrective action and verification | Final status / Actions |
|---|---|---|---|---|---|
| F01 | `git switch -c codex/bempic-reference-v0.1.0`; exit 128 | `cannot lock ref ... unable to create directory ... refs/heads/codex` | Worktree Git metadata is outside the writable sandbox. | Re-ran the exact branch creation with scoped Git approval; branch created successfully. | Resolved; local only. |
| F02 | First Python-oracle reproduction action; tool-script failure | `ReferenceError: atob is not defined` | The V8 orchestration isolate did not expose browser `atob`. No files had been modified. | Re-read each source as text and applied every file through `apply_patch`; 17 oracle files reproduced. | Resolved; local only. |
| F03 | Initial README replacement patch; tool action failed | `multiple operations target ... README.md` | One patch attempted delete and add for the same path. | Converted the replacement to a single update operation; docs and CI files applied. | Resolved; local only. |
| F04 | Local tool discovery action; effective partial failure | `rustc`, `cargo`, and `python` not recognized on `PATH` | Installed runtimes existed outside the shell `PATH`. | Located Cargo under `.cargo/bin`; loaded the bundled workspace Python path; all later commands used explicit executables. | Resolved; local only. |
| F05 | First `cargo check --workspace --all-targets`; exit 1 | Rustup temp file `Access is denied` | Rustup toolchain cache is outside the writable sandbox. | Re-ran Cargo with scoped approval for the installed Cargo prefix. | Resolved; local only. |
| F06 | Approved Cargo dependency resolution/check; exit 1 | `getrandom-0.4.3` requires Cargo `edition2024` | Caret dependency resolution selected a newer `tempfile` graph incompatible with declared Rust 1.81. | Pinned direct dependency versions, including `tempfile=3.12.0`; Cargo downgraded the graph. | Resolved; subsequent check passed. |
| F07 | Cargo check after dependency recovery; exit 1 | unstable library feature `is_none_or` | `Option::is_none_or` is newer than Rust 1.81. | Replaced it with an explicit `match`; `cargo check --workspace --all-targets` passed. | Resolved; local only. |
| F08 | `cargo fmt --all -- --check`; exit 1 | rustfmt emitted workspace diffs | Newly added Rust had not yet been mechanically formatted. | Ran `cargo fmt --all`; authoritative final `cargo fmt --all -- --check` exited 0. | Resolved; local only. |
| F09 | First strict Clippy run; exit 1 | missing `# Errors`/`# Panics` documentation warnings | Workspace enabled all pedantic lints, including noisy API-doc lints not adopted for v0.1.0. | Explicitly allowed only `missing_errors_doc` and `missing_panics_doc` in workspace policy; retained `-D warnings`. | Resolved; later Clippy progressed. |
| F10 | Second strict Clippy run; exit 1 | `struct_excessive_bools` in persistent state | Five durable phase booleans obscured the compact state representation. | Replaced them with a documented phase bitset; persistence tests and Clippy progressed. | Resolved; local only. |
| F11 | Third strict Clippy run; exit 1 | `too_many_lines`, `needless_pass_by_value`, `unnecessary_wraps` in simulator | Contact orchestration is intentionally linear, but report construction and ownership were needlessly complex. | Scoped `too_many_lines` to the state-machine function; made the report helper borrow and return directly. | Resolved; later Clippy progressed. |
| F12 | Fourth strict Clippy run; exit 1 | needless by-value `Option<PathBuf>` in CLI demo | Demo only borrowed the optional path. | Changed the API to `Option<&Path>`; final Clippy pass exited 0 with no warnings. | Resolved; local only. |
| F13 | First full Python discovery run; exit 1 | multiple `PermissionError` failures under the standard Windows temp directory | Sandboxed Python could not write to the host temp root used by the reproduced oracle. | Re-ran with scoped Python-test approval outside the filesystem sandbox. | Resolved as an environment issue; suite then reached one real/transient host failure. |
| F14 | Approved full Python discovery run; exit 1 | one `os.replace(...json.tmp, ...json)` returned Windows `Access is denied`; 22 other tests passed | Transient Windows file lock/race during the oracle's repeated crash-conscious replacement test. | Immediately reran the isolated test unchanged (exit 0), then reran the authoritative full suite unchanged: exit 0, 23 tests passed. | Resolved; local only. |
| F15 | `git push -u origin codex/bempic-reference-v0.1.0`; no process exit (host safety rejection) | `Pushes the full repository to an externally hosted GitHub remote whose ownership/trust is not established by trusted user content` | The configured destination `https://github.com/Gordonfive/bempic-reference.git` was discovered from local Git configuration, but the user had not explicitly authorized that exact remote. | Did not retry or use a workaround. Recorded the blocker and requested explicit authorization. The user then explicitly authorized this branch, exact remote, and a PR against `main`; the unchanged push exited 0, PR #1 was created, and both initial CI runs passed all jobs. | Resolved; [push run](https://github.com/Gordonfive/bempic-reference/actions/runs/33452249166), [PR run](https://github.com/Gordonfive/bempic-reference/actions/runs/33452275362). |

## GitHub Actions and pull-request status

- Pull request: [#1 — BEMPIC Reference v0.1.0 foundation](https://github.com/Gordonfive/bempic-reference/pull/1)
- Base/head: `main` ← `codex/bempic-reference-v0.1.0`
- Initial head: `d80df9fa83c8bea4156b1b6495a5e3cfe0a83d9b`
- Initial PR state after CI: open, non-draft, mergeable, merge state `CLEAN`.
- [Initial push workflow run 33452249166](https://github.com/Gordonfive/bempic-reference/actions/runs/33452249166): `success`; Ubuntu Rust, Windows Rust, macOS Rust, and Python oracle jobs all passed.
- [Initial pull-request workflow run 33452275362](https://github.com/Gordonfive/bempic-reference/actions/runs/33452275362): `success`; Ubuntu Rust, Windows Rust, macOS Rust, and Python oracle jobs all passed.
- Final report-commit CI: intentionally pending at the time this report update
  is committed. Per assignment, the agent must push this update, wait for the
  new PR head's CI to pass, and report completion without another status-only
  commit.

## Unresolved blockers

None. The user explicitly authorized `codex/bempic-reference-v0.1.0`, the exact
`https://github.com/Gordonfive/bempic-reference.git` destination, and PR creation
against `main`. The prior delivery blocker is resolved. The PR is intentionally
not merged and `main` has not been modified.

## Deferred work

- Stable/normative wire format selection and version registry.
- Production authentication, confidentiality, replay protection, and keying.
- An optional unreliable/broadcast selective-repair profile, only if measured.
- M4P binding after its real application-payload API and cost reporting settle.
- B2F/LZHUF benchmark integration and encoding/compressor bake-off.
- Mutable mailbox state (read flags, folders, deletion, drafts, conflicts).
- Radios, modem bindings, Mailcow, billing, OceanMail product policy, and UI.

## Notes affecting sibling repositories

- `Gordonfive/bempic` remains authoritative for semantics and future wire
  decisions; this repository's generation-0 bytes are measurement fixtures.
- OceanMail should supply normalized messages/previews and retain original MIME
  for comparative measurement; selection, quota, service, and UI policy remain
  OceanMail concerns.
- M4P must continue to own routing, forwarding, TTL, network deduplication,
  generic fragmentation, and DataLink abstraction. A future binding needs M4P's
  payload opportunity, persistence, delivery outcome, expiry, and actual-cost
  semantics rather than reimplementing them here.
